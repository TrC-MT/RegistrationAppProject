All fetch statements

Implement the loading bar to places that should have it.

Add logging.

Find a better way to fix the admin courses search.  The variable num is just one multiple too big every other time.
