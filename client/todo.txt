All fetch statements

Implement the loading bar to places that should have it.

Add logging.

Test if admins can actually manage student courses.

Students see registered courses first

Find a better way to fix the admin courses search.  The variable num is just one multiple too big every other time.    (https://react.dev/reference/react-dom/components/input#controlling-an-input-with-a-state-variable)
